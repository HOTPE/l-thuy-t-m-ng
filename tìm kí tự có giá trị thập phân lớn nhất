#include <iostream>
#include <string>
using namespace std;
int checkmax(string str)
{
	int max = str[0];
	for (int i = 0; i < str.size(); i++)
	{
		if (str[i] > max)
			max = str[i];
	}
	return max;
}
int main()
{
	string a;
	getline(cin, a);
	cout << checkmax(a) << endl;
	return 0;
}
